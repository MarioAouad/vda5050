# Pinned to the exact versions from the working conda environment
# (requirements_exact.txt) for the same reason as agent-system-a's
# requirements.txt — see the comment there.

# RAG pipeline
langchain-community==0.4.2
langchain-core==1.5.1
langchain-classic==1.0.8
langchain-text-splitters==1.1.2
rank-bm25==0.2.2

# MCP server (streamable-HTTP transport)
# mcp 2.0.0 renamed/restructured mcp.server.fastmcp.FastMCP — stay below it.
mcp[cli]==1.29.0

# Vector database & embeddings
langchain-huggingface==1.2.2
langchain-qdrant==1.1.0
qdrant-client==1.18.0
sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1
numpy==2.0.1

# Utilities & config
pydantic==2.13.4
python-dotenv==1.2.2
