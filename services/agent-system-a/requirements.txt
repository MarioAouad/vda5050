# Pinned to the exact versions from the working conda environment
# (requirements_exact.txt, `pip freeze` from the pre-Docker prototype) rather
# than open-ended ">=" ranges — the loose ranges let pip resolve arbitrary
# "latest" versions during the Docker build, which is what caused the
# AttributeError: content sometimes came back as a list instead of a string,
# from a langchain-google-genai version behaving differently than the one
# actually tested during development.

# Orchestration
langchain-core==1.5.1
langchain-groq==1.1.3
langchain-google-genai==4.3.2
langgraph==1.2.9
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.1

# MCP client (connects to the mcp-server container over streamable-HTTP)
mcp[cli]==1.29.0
langchain-mcp-adapters==0.3.1

# HTTP client (agent-system-a -> agent-system-b diagnostics/validation calls)
httpx==0.28.1

# Utilities & config
pydantic==2.13.4
python-dotenv==1.2.2

# Deterministic per-turn reply-language detection (see graph.py's
# _detect_reply_language) — pure-Python, no model download, offline at
# runtime.
langdetect==1.0.9

# FastAPI & web framework
fastapi==0.141.1
uvicorn[standard]==0.52.1
